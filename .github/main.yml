on: [push]

jobs:
  schema_check_job:
    runs-on: ubuntu-latest
    name: A job to check schema
    steps:
      - uses: VitaliiBorodii/schema_check_test@v1.0.1
        with:
          token: '123'
      - run: echo random-number ${{ steps.foo.outputs }}
        shell: bash